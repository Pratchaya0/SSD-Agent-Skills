{
  "name": "ssd-agent-skills",
  "version": "1.0.0",
  "description": "SSD team coding standards as agent skills — React/TS, .NET Core, Python, Git",
  "type": "module",
  "scripts": {
    "build": "node scripts/build.js",
    "rebuild": "node -e \"import('fs').then(fs=>fs.default.rmSync('dist',{recursive:true,force:true}))\" && node scripts/build.js",
    "clean": "node -e \"import('fs').then(fs=>fs.default.rmSync('dist',{recursive:true,force:true}))\"",
    "test": "node --test tests/build.test.mjs && node --test tests/skill-router.test.mjs && node --test tests/skill-content.test.mjs"
  },
  "engines": {
    "node": ">=18"
  }
}
